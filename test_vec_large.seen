import seen_std.collections.vec { Vec }

fun main() {
    let size = 312500;
    let v = Vec<Int>.withCapacity(size);
    var i = 0;
    while i < size {
        v.push(i);
        i = i + 1;
    }
    println("Done");
}
