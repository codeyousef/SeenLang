// First definition
struct World {
    count: Int
}

// Second definition (simulating what happens with inline defs)
struct World {
    count: Int
}

fun test(w: World) -> Int {
    return w.count
}

fun main() -> Int {
    let w = World{ count: 5 }
    return test(w)
}
