///
: Feature flags — @cfg(feature: "test_feature")
Compile with: --feature=test_feature
///

@cfg(feature: "test_feature")
fun featureEnabled() r: Int {
    return 42
}

@cfg(feature: "nonexistent")
fun featureDisabled() r: Int {
    return 99
}

fun main() r: Int {
    ///
    featureEnabled should be included when --feature=test_feature
    featureDisabled should be skipped (predicate not met)
    ///
    println("PASS: Feature flags test (compile-only)")
    return 0
}
