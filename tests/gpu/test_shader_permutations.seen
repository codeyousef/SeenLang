///
Test: Shader permutations via @cfg(feature:)
Verifies GLSL generator produces multiple SPIR-V variants
///

@compute(workgroup: 64)
fun particleUpdate(particles: Buffer<Float>, dt: Float) {
    let idx = globalInvocationId.x

    // Base update
    particles[idx] = particles[idx] + dt

    ///
    @cfg(feature: "gravity") — generates variant with gravity enabled
    @cfg(feature: "wind") — generates variant with wind enabled
    ///
}

fun main() r: Int {
    println("test_shader_permutations: @cfg feature-based variants")
    println("  Base shader + feature variants")
    println("PASS")
    return 0
}
