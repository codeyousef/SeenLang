class Point {
    var x: Int
    var y: Int
}

fun Point_new(x: Int, y: Int) r: Point {
    var p = Point()
    p.x = x
    p.y = y
    return p
}

fun main() r: Int {
    let p = Point_new(3, 4)
    __PrintInt(p.x + p.y)
    __Print("\n")
    return 0
}
