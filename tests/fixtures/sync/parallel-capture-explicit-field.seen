@share
class SharedOwner {
    let offset: Int

    fun visit() r: Int {
        parallel_for index in 0..4 {
            println(index + this.offset)
        }
        return 0
    }
}

fun main() r: Int { return 0 }
