manifest-version = 1

[project]
name = "external_package"
version = "0.1.0"
language = "en"
modules = ["src/mod.seen"]
license = "MIT"
repository = "https://github.com/codeyousef/SeenLang"

[package]
identity = "seen/external-package"
visibility = "public"
include = ["src/**/*.seen", "examples/**/*.seen", "README.md", "LICENSE"]
assets = []
capabilities = []

[dependencies]

[native.dependencies]
