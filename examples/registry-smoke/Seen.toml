manifest-version = 1

[project]
name = "registry_smoke"
version = "0.1.0"
license = "MIT"
repository = "https://github.com/codeyousef/SeenLang"

[package]
identity = "seen/registry-smoke"
visibility = "public"
include = ["src/**/*.seen"]
assets = []
capabilities = []

[dependencies]
