[project]
name = "seen-ecs-min"
version = "0.1.0"
language = "en"
description = "Deterministic ECS workload for the Seen MVP."

[build]
entry = "src/main.seen"
targets = ["x86_64-unknown-linux-gnu", "wasm32-unknown-unknown", "android-arm64"]
optimize = "speed"
