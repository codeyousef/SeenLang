
class Type {
    var name: String
    var isNullable: Bool
}

class Environment {
    var parent: Environment?

    fun new(parent: Environment? = null) r: Environment {
        let env = Environment()
        env.parent = parent
        return env
    }
}

fun main() r: Int {
    let e = Environment.new()
    return 0
}
