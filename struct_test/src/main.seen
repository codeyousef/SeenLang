// Struct test program

struct Point {
    x: Int,
    y: Int
}

func main() {
    val p = Point { x: 10, y: 20 };
    val x_val = p.x;
    val y_val = p.y;
    
    println(x_val);
    println(y_val);
}
