// Array bounds checking test

func main() {
    val numbers = [1, 2, 3];
    
    // Valid access
    val first = numbers[0];
    println(first);
    
    // Valid access
    val second = numbers[1];
    println(second);
}
