import env.env.{args}

fun main() r: Int {
    let rawArgs = args()
    var list: List<String> = []
    var index = 0
    while index < rawArgs.length() {
        list.push(rawArgs[index])
        index = index + 1
    }
    return list.length()
}
