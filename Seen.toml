manifest-version = 1

[project]
name = "seenlang"
version = "0.14.0"
language = "en"
description = "Seen language compiler and standard library workspace"

[build]
targets = ["native"]
optimize = "speed"
