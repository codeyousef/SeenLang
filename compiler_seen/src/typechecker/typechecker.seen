class SeenTypeChecker {
    var smartCastContext: Map<String, Any>
    var strictMode: Bool
    var environment: Map<String, Any>
    var errors: Array<String>
    var warnings: Array<String>

    static fun new() r: SeenTypeChecker {
        return SeenTypeChecker{
            smartCastContext: Map<String, Any>.new(),
            strictMode: true,
            environment: Map<String, Any>.new(),
            errors: Array<String>(),
            warnings: Array<String>()
        }
    }

    fun check(program: Any) r: Bool {
        println("[typechecker-core] noop")
        return true
    }
}
