fun demo() r: Int {
    var hash = 5
    var remaining = if hash < 0 { 0 - hash } else { hash }
    var digits = 0
    while digits < 2 {
        remaining = remaining + digits
        digits = digits + 1
    }
    return remaining
}

fun main() r: Int {
    return demo()
}