class Point {
    var x: Int
    var y: Int
}

fun main() {
    let p = Point { x: 1, y: 2 }
    p.x = 3
    if p.x == 1 {
        abort("p.x is still 1")
    }
    if p.x == 3 {
        abort("p.x is 3")
    }
    abort("p.x is something else")
}
