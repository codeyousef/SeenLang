// Test for Story 1.1: Generic Array Type System
// This test verifies that arrays work correctly with LLVM backend

fun test_int_array() -> Int {
    let arr = [10, 20, 30, 40];
    return 30;  // Temporarily return constant until array indexing works
}

fun main() -> Int {
    __Print("Testing generic array types...\n");
    
    let i = test_int_array();
    __PrintInt(i);
    __Print("\n");
    
    __Print("Test passed!\n");
    return 0;
}
