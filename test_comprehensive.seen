// Test what actually works in Seen

// Basic variables and arithmetic
let x = 10
let y = 20
let sum = x + y

// Conditionals
if sum > 25 {
    let result = sum * 2
} else {
    let result = sum / 2
}

// While loop
var counter = 0
while counter < 5 {
    counter = counter + 1
}

// For loop
var total = 0
for i in 1..5 {
    total = total + i
}

// Arrays
let numbers = [1, 2, 3, 4, 5]
let first = numbers[0]
let second = numbers[1]

// Functions
fun add(a: Int, b: Int): Int {
    return a + b
}

let result = add(10, 20)

// Return final value
result + total + counter
