# Scripting

Module: `scripting/lua`

The scripting module provides Lua integration wrappers.

| Type | Purpose |
|------|---------|
| `LuaState` | Lua runtime state wrapper |
The primary public type is `LuaState`, an opaque wrapper around the Lua runtime
state and helper operations.
