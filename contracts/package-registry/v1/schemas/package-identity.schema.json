{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:seen:package-registry:v1:package-identity",
  "title": "Seen package identity",
  "description": "Canonical lowercase ASCII owner/name identity. Authorization and reserved-name policy are evaluated separately.",
  "type": "string",
  "minLength": 3,
  "maxLength": 127,
  "pattern": "^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?/[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$"
}
