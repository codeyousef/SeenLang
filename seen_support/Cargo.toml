[package]
name = "seen_support"
version = "0.1.0"
edition = "2021"

[features]
default = []
serde = ["dep:serde"]

[dependencies]
thiserror = "1.0.69"
serde = { version = "1.0", features = ["derive"], optional = true }
anyhow = "1.0.98"
